###NOTE: 
The file "ethical_or_unethical" cannot be executed in VS Code because it is written in a language specific to another software environment. Its syntax is not compatible with Visual Studio Code’s built‑in runtimes. To run it, download the file directly, open it and copy and paste the code into your NetLogo environment.
